import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.rotion63.friendlysplitter',
  appName: 'कति भो बिल ?',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
 };

export default config;
